/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },

  assetPrefix: "",
  basePath: "/aidriven.ieeeyp.lk",
  output: "export",
};

export default nextConfig;
